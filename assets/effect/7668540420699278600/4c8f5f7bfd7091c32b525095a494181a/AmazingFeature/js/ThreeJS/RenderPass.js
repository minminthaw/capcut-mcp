class RenderPass{constructor(scene,camera){this.scene=scene;this.camera=camera;this.enabled=true;this.needsSwap=false;this.clear=false;this.renderToScreen=false}setSize(width,height){}render(renderer,writeBuffer,readBuffer,deltaTime,maskActive){if(!this.enabled||!renderer||!this.scene||!this.camera)return;const target=this.renderToScreen||!writeBuffer?null:writeBuffer;if(typeof renderer.setRenderTarget==="function"){renderer.setRenderTarget(target)}if(this.clear&&typeof renderer.clear==="function"){renderer.clear()}renderer.render(this.scene,this.camera)}dispose(){}}exports.RenderPass=RenderPass;