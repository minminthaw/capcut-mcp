class Euler{constructor(x=0,y=0,z=0,order="XYZ"){this.x=x;this.y=y;this.z=z;this.order=order}set(x,y,z,order){this.x=x;this.y=y;this.z=z;this.order=order||this.order;return this}setFromUnitVectors(vFrom,vTo){const from=vFrom.clone().normalize();const to=vTo.clone().normalize();const pitch=Math.asin(to.y);const yaw=Math.atan2(to.x,to.z);const roll=0;this.x=pitch;this.y=-yaw;this.z=roll;return this}setFromQuaternion(q,order){this.x=q.x;this.y=q.y;this.z=q.z;this.order=order||this.order;return this}clone(){return new Euler(this.x,this.y,this.z,this.order)}}exports.Euler=Euler;